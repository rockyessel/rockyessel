import React from 'react';
import { Layout } from '@/components';

const Contact = () => {
  return (
    <Layout
      description={''}
      title={''}
      image={''}
      type={''}
      alt={''}
      keywords={''}
      publishedAt={''}
      updatedAt={''}
      MIME={''}
      author_name={''}
    >
j
    </Layout>
  );
};

export default Contact;
